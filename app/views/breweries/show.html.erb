
<%= render @brewery %>
<br>
<div>
  <%= link_to "change activity", toggle_activity_brewery_path(@brewery.id), data: {turbo_method: "post"}, class: "btn btn-secondary" %>
  <%= link_to('Edit this brewery', edit_brewery_path(@brewery), class: "btn btn-primary") if current_user %>
  <%= link_to "Back to breweries", breweries_path, class: "btn btn-primary" %>
</div>
<br>
<%= button_to('Destroy this brewery', @brewery, method: :delete, class: "btn btn-danger") if current_user %>
